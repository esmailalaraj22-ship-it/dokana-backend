/**
 * Business-table mappings are intentionally deferred.
 *
 * The approved PostgreSQL all-in-one SQL is already applied to a non-empty
 * database. Table mappings must be reviewed against that baseline before any
 * Drizzle migration is generated.
 */
export {};
